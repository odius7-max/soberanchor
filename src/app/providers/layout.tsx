// Provider routes now use the main layout and nav.
// The ProviderNav component has been retired as part of the unified dashboard.
export default function ProvidersLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
